{
  "name": "wordle-export",
  "version": "1.0.0",
  "description": "Allows a user to import/export their user stats between devices",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.js"
  },
  "keywords": [],
  "author": "James Martin (@ymhr)",
  "license": "ISC",
  "devDependencies": {
    "terser": "^5.10.0"
  }
}
